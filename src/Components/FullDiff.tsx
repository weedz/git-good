import { h, Component } from "preact";

export default class FullDiff extends Component {
    render() {
        return (
            <div id="diff-pane" class="pane">
                <h4>Diff</h4>
            </div>
        );
    }
}
